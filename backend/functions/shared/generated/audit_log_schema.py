# generated by datamodel-codegen:
#   filename:  audit-log.schema.json

from __future__ import annotations

from enum import StrEnum
from typing import Any

from pydantic import AwareDatetime, BaseModel, ConfigDict, EmailStr, constr


class Action(StrEnum):
    create = 'create'
    update = 'update'
    delete = 'delete'
    publish = 'publish'
    unpublish = 'unpublish'
    reorder = 'reorder'


class AuditLog(BaseModel):
    """
    Who changed what, when. Written by the API on every admin mutation. Never client-readable.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    actor: constr(min_length=1, max_length=128)
    """
    Firebase Auth uid.
    """
    actorEmail: EmailStr | None = None
    action: Action
    collection: constr(min_length=1, max_length=60)
    docId: constr(max_length=128) | None = None
    before: dict[str, Any] | None = None
    after: dict[str, Any] | None = None
    at: AwareDatetime
