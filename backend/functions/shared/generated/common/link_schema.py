# generated by datamodel-codegen:
#   filename:  common/link.schema.json

from __future__ import annotations

from pydantic import AnyUrl, BaseModel, ConfigDict, constr

from ..enums import link_type


class Link(BaseModel):
    """
    An outbound link. Links are stored as an array of these rather than a fixed object, so a new kind of link needs no schema change.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    type: link_type.LinkType
    label: constr(min_length=1, max_length=60) | None = None
    url: AnyUrl
    primary: bool | None = False
    """
    At most one per record. Rendered as the main call to action.
    """
