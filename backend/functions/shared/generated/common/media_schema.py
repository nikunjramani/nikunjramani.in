# generated by datamodel-codegen:
#   filename:  common/media.schema.json

from __future__ import annotations

from pydantic import AnyUrl, BaseModel, ConfigDict, conint, constr

from ..enums import media_type


class Media(BaseModel):
    """
    An image or video. Dimensions are stored so the frontend can reserve space and avoid layout shift; blurhash gives a placeholder while the real file loads.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    url: AnyUrl
    alt: constr(min_length=1, max_length=200)
    """
    Required on every image. The admin panel will not let one be used without it.
    """
    caption: constr(max_length=300) | None = None
    type: media_type.MediaType | None = 'image'
    width: conint(ge=1) | None = None
    height: conint(ge=1) | None = None
    blurhash: constr(max_length=64) | None = None
